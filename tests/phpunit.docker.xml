<?xml version="1.0" encoding="utf-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         bootstrap="bootstrap.php"
         timeoutForSmallTests="900"
         timeoutForMediumTests="900"
         timeoutForLargeTests="900"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/9.3/phpunit.xsd"
         colors="true">
  <testsuites>
    <!--
      Only the unit suite runs here. Unit tests mock every dependency, so they
      never touch a real database. Integration tests (tests/integration) issue
      real DELETE/INSERT statements and MUST NOT run against a live instance —
      run them explicitly via tests/phpunit.integration.xml against a throwaway DB.
    -->
    <testsuite name="Forum Tests">
      <directory suffix="Test.php">unit</directory>
    </testsuite>
  </testsuites>
</phpunit>
